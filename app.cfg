/**
 * EV3RT TOPPERS kernel configuration for the line follower application.
 *
 * Defines the main task with standard priority and stack size.
 */

INCLUDE("app_common.cfg");

#include "app.h"

CRE_TSK(MAIN_TASK, { TA_ACT, 0, main_task, TMIN_APP_TPRI + 1, STACK_SIZE, NULL });
